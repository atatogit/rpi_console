/* CREATE USER 'nobody'@'localhost' IDENTIFIED BY 'nobody'; */

/* CREATE DATABASE IF NOT EXISTS sensors; */

/* GRANT SELECT ON sensors.* to nobody@localhost identified by 'nobody'; */

/* GRANT INSERT ON sensors.* to nobody@localhost identified by 'nobody'; */

/*
CREATE TABLE IF NOT EXISTS sensors.dht22readings (
  device_id INT UNSIGNED,
  ts_secs INT UNSIGNED,
  temperature_c FLOAT,
  humidity_perc FLOAT,
  PRIMARY KEY(device_id, ts_secs)
);
*/
